# Database (port 5433 to avoid conflict with local PostgreSQL)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=bullish_clash
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/bullish_clash

# Redis
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=bullish-clash-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# API
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Frontend (Next.js public variables)
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000

# Production URLs (update these for deployment)
API_URL=http://localhost:4000
WS_URL=ws://localhost:4000

# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=subedirohit49@gmail.com
SMTP_PASS=wwtr ymcs hjxc cigg
SMTP_FROM=noreply@bullishclash.com
APP_URL=http://localhost:3000
